# Standards: 0.1
---
# @var discord_user:description: User to run user-specific commands
# @var discord_user:value: discord_user | default(homebrew_user) | default(ansible_user_id)
# discord_user:

# @var discord_started:description: Start in background after install
discord_started: True

...
